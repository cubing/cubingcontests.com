apiVersion: v1
kind: Service
metadata:
  name: recordranks-service
spec:
  type: NodePort
  selector:
    app: recordranks-nextjs
  ports:
    - name: recordranks-port
      protocol: TCP
      port: 3000
      targetPort: nextjs-port
