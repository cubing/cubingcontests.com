info:
  name: events
  type: http
  seq: 2

http:
  method: GET
  url: "{{apiBaseUrl}}/{{spaceId}}/events"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
