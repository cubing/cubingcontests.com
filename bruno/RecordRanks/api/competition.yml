info:
  name: competition
  type: http
  seq: 4

http:
  method: GET
  url: "{{apiBaseUrl}}/{{spaceId}}/competitions/MyCompetition2023"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
